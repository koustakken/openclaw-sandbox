# Docker draft

This folder contains a commented draft (`docker-compose.draft.yml`) for future containerization.

When you are ready, we can add:
- `Dockerfile.api`
- `Dockerfile.web`
- production and dev compose files
